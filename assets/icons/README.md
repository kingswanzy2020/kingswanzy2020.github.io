Drop real vendor SVGs here (see add-real-icons.sh in the repo root for the
exact filenames and where to source each one), then run:

    ./add-real-icons.sh index.html

to switch the diagram over from placeholder glyphs to real icons.
